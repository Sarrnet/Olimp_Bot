services:
    bot:
        build:
            context: .
            args:
                DATABASE_URL: postgresql://${POSTGRES_USER:-user}:${POSTGRES_PASSWORD:-password}@db:5432/${POSTGRES_DB:-olimp_bot}?schema=public
        restart: always
        env_file: .env
        logging:
            driver: 'json-file'
            options:
                max-size: '10m'
                max-file: '3'
        depends_on:
            db:
                condition: service_healthy
            redis:
                condition: service_started
        environment:
            DATABASE_URL: postgresql://${POSTGRES_USER:-user}:${POSTGRES_PASSWORD:-password}@db:5432/${POSTGRES_DB:-olimp_bot}?schema=public
            REDIS_URL: redis://redis:6379

    db:
        image: postgres:18-alpine
        restart: always
        ports:
            - '5433:5432'
        environment:
            POSTGRES_USER: ${POSTGRES_USER:-user}
            POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-password}
            POSTGRES_DB: ${POSTGRES_DB:-olimp_bot}
        volumes:
            - postgres_data:/var/lib/postgresql
        healthcheck:
            test: ['CMD-SHELL', 'pg_isready -U ${POSTGRES_USER:-user} -d ${POSTGRES_DB:-olimp_bot}']
            interval: 5s
            timeout: 5s
            retries: 5

    redis:
        image: redis:8-alpine
        restart: always
        volumes:
            - redis_data:/data

volumes:
    postgres_data:
    redis_data:
